<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item" href="https://bulma.io">
      <img src="https://bulma.io/images/bulma-logo.png" width="112" height="28">
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to "Films", films_path,class: 'navbar-item' %>

      <%= link_to "Edit profile", edit_user_path(current_user),class: 'navbar-item' if user_signed_in? %>
    </div>
    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            <%= button_to "Log out", destroy_user_session_path, method: :delete, class: 'button' %>
          <% else %>
            <%= link_to '<strong>Sign up</strong>'.html_safe, new_user_registration_path, class: 'button is-primary' %>
            <%= link_to 'Log in', new_user_session_path, class: 'button is-light' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>