<h1>New comment</h1>

<%= form_with model: @comment, url: film_comments_path(@comment.film_id) do |f| %>
  <h2>Create Comments</h2>
  <div>
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>
  <%= f.submit "Create Comment" %>
<% end %>